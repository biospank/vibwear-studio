apply plugin: 'com.android.application'

android {
    compileSdkVersion 21
    buildToolsVersion "21.1.2"

    defaultConfig {
        applicationId "it.lampwireless.vibwear.app"
        minSdkVersion 18
        targetSdkVersion 21
        versionCode 11
        versionName "1.3.7"
    }

    buildTypes {
        release {
            minifyEnabled true
        }
    }

    lintOptions {
        abortOnError false
    }
}

dependencies {
    compile 'com.mbientlab:metawear:1.6.21'
    compile 'com.google.code.gson:gson:2.3'
}
